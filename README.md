# CPlusPlus-LinearSearch


The C++ code prompts the user to input 10 numbers, searches for a specific key value within the array, and prints its index if found; otherwise, it indicates that the key is not found.
